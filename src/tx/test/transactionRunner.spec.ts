// This test requires external ethereum-tests directory which is not available

import { describe, it } from 'vitest'

// Skipping this test suite
describe.skip('TransactionTests', () => {
  it('skipped - requires external ethereum-tests directory', () => {})
})
